@using UI.Models;
@model SearchResultViewModel<OrderModel,OrderFilterModel>;
@{
    ViewData["Title"] = "Главная страница";
}

<!--Фильтр-->

<partial name="_Filter" for="FilterModel" />
<!---->
<a href="@Url.ActionLink("Update", "Home")">Добавить</a>

<table>
  <tr>
    <td>Номер</td>
    <td>Дата</td>
    <td>Провайдер</td>
    <td>Изменить</td>
    <td>Удалить</td>
  </tr>
  @foreach(var el in Model.Objects)
    {
       <tr>
            <td>@el.Number</td>
            <td>@el.Date</td>
            <td>@el.Id</td>
            <td><a href="@Url.ActionLink("Update", "Home", new {id = @el.Id})">Изменить</a></td>
            <td><a href="@Url.ActionLink("Delete", "Home", new {id = @el.Id})">Удалить</a></td>
       </tr> 
    }
</table>



